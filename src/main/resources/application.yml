spring:
  datasource:
    url: jdbc:mysql://localhost:3306/custom_queries_2
    username: fconsiglio98
    password: inserirePassword
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  jackson:
    serialization:
      fail-on-empty-beans: false
